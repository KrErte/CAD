# cp -> secret.yaml, fill in real values, then `kubectl apply -f secret.yaml`
# secret.yaml is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: ai-cad-secrets
  namespace: ai-cad
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "sk-ant-..."
  MESHY_API_KEY: ""
